apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.forms.name }}
  {{- if eq (.Values.ingress.controller | default "nginx-f5") "alb" }}
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: {{ .Values.forms.healthcheck }}
  {{- else if or (eq (.Values.ingress.controller | default "nginx-f5") "gce") (eq (.Values.ingress.controller | default "nginx-f5") "gce-internal") }}
  annotations:
    cloud.google.com/backend-config: '{"default": "{{ .Values.forms.name }}-backend-config"}'
  {{- end }}
spec:
  type: {{ .Values.serviceType | default "ClusterIP" }}
  ports:
    - protocol: TCP
      port: {{ .Values.forms.port }}
      targetPort: {{ .Values.forms.port }}
  selector:
    integrify.service: {{ .Values.forms.name }}
status:
  loadBalancer: {}
