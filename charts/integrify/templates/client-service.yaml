apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.client.name }}
  {{- if eq (.Values.ingress.controller | default "nginx-f5") "alb" }}
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: {{ .Values.client.healthcheck }}
  {{- else if or (eq (.Values.ingress.controller | default "nginx-f5") "gce") (eq (.Values.ingress.controller | default "nginx-f5") "gce-internal") }}
  annotations:
    cloud.google.com/backend-config: '{"default": "{{ .Values.client.name }}-backend-config"}'
  {{- end }}
spec:
  type: {{ .Values.serviceType | default "ClusterIP" }}
  selector:
    integrify.service: {{ .Values.client.name }}
  ports:
    - protocol: TCP
      port: {{ .Values.client.port }}
      targetPort: {{ .Values.client.port }}
status:
  loadBalancer: {}
