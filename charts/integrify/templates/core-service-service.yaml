apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.coreService.name }}
  {{- if eq (.Values.ingress.controller | default "nginx-f5") "alb" }}
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: {{ .Values.coreService.healthcheck }}
  {{- else if or (eq (.Values.ingress.controller | default "nginx-f5") "gce") (eq (.Values.ingress.controller | default "nginx-f5") "gce-internal") }}
  annotations:
    cloud.google.com/backend-config: '{"default": "{{ .Values.coreService.name }}-backend-config"}'
  {{- end }}
spec:
  type: {{ .Values.serviceType | default "ClusterIP" }}
  selector:
    integrify.service: {{ .Values.coreService.name }}
  ports:
    - protocol: TCP
      port: {{ .Values.coreService.port }}
      targetPort: {{ .Values.coreService.port }}
status:
  loadBalancer: {}
