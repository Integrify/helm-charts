apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.processes.name }}
  {{- if eq (.Values.ingress.controller | default "nginx-f5") "alb" }}
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: {{ .Values.processes.healthcheck }}
  {{- else if or (eq (.Values.ingress.controller | default "nginx-f5") "gce") (eq (.Values.ingress.controller | default "nginx-f5") "gce-internal") }}
  annotations:
    {{- if eq (.Values.serviceType | default "ClusterIP") "NodePort" }}
    cloud.google.com/backend-config: '{"default": "{{ .Values.processes.name }}-backend-config"}'
    {{- else }}
    cloud.google.com/backend-config: '{"ports": {"{{ .Values.processes.port }}": "{{ .Values.processes.name }}-backend-config"}}'
    {{- end }}
  {{- end }}
spec:
  type: {{ .Values.serviceType | default "ClusterIP" }}
  selector:
    integrify.service: {{ .Values.processes.name }}
  ports:
    - protocol: TCP
      port: {{ .Values.processes.port }}
      targetPort: {{ .Values.processes.port }}
status:
  loadBalancer: {}
