apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.instance.name }}
  {{- if eq (.Values.ingress.controller | default "nginx-f5") "alb" }}
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: {{ .Values.instance.healthcheck }}
  {{- else if or (eq (.Values.ingress.controller | default "nginx-f5") "gce") (eq (.Values.ingress.controller | default "nginx-f5") "gce-internal") }}
  annotations:
    cloud.google.com/backend-config: '{"default": "{{ .Values.instance.name }}-backend-config"}'
  {{- end }}
spec:
  type: {{ .Values.serviceType | default "ClusterIP" }}
  selector:
    integrify.service: {{ .Values.instance.name }}
  ports:
    - protocol: TCP
      port: {{ .Values.instance.port }}
      targetPort: {{ .Values.instance.port }}
status:
  loadBalancer: {}
