apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.taskDispatcher.name }}
  {{- if eq (.Values.ingress.controller | default "nginx-f5") "alb" }}
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: {{ .Values.taskDispatcher.healthcheck }}
  {{- else if or (eq (.Values.ingress.controller | default "nginx-f5") "gce") (eq (.Values.ingress.controller | default "nginx-f5") "gce-internal") }}
  annotations:
    cloud.google.com/backend-config: '{"default": "{{ .Values.taskDispatcher.name }}-backend-config"}'
  {{- end }}
spec:
  type: {{ .Values.serviceType | default "ClusterIP" }}
  selector:
    integrify.service: {{ .Values.taskDispatcher.name }}
  ports:
    - protocol: TCP
      port: {{ .Values.taskDispatcher.port }}
      targetPort: {{ .Values.taskDispatcher.port }}
status:
  loadBalancer: {}
