apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.tenant.name }}
  {{- if eq (.Values.ingress.controller | default "nginx-f5") "alb" }}
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: {{ .Values.tenant.healthcheck }}
  {{- else if or (eq (.Values.ingress.controller | default "nginx-f5") "gce") (eq (.Values.ingress.controller | default "nginx-f5") "gce-internal") }}
  annotations:
    {{- if eq (.Values.serviceType | default "ClusterIP") "NodePort" }}
    cloud.google.com/backend-config: '{"default": "{{ .Values.tenant.name }}-backend-config"}'
    {{- else }}
    cloud.google.com/backend-config: '{"ports": {"{{ .Values.tenant.port }}": "{{ .Values.tenant.name }}-backend-config"}}'
    {{- end }}
  {{- end }}
spec:
  type: {{ .Values.serviceType | default "ClusterIP" }}
  selector:
    integrify.service: {{ .Values.tenant.name }}
  ports:
    - protocol: TCP
      port: {{ .Values.tenant.port }}
      targetPort: {{ .Values.tenant.port }}
status:
  loadBalancer: {}
