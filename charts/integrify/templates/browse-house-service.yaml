apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.browseHouse.name }}
  {{- if eq (.Values.ingress.controller | default "nginx-f5") "alb" }}
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: {{ .Values.browseHouse.healthcheck }}
  {{- else if or (eq (.Values.ingress.controller | default "nginx-f5") "gce") (eq (.Values.ingress.controller | default "nginx-f5") "gce-internal") }}
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
    {{- if eq (.Values.serviceType | default "ClusterIP") "NodePort" }}
    cloud.google.com/backend-config: '{"default": "{{ .Values.browseHouse.name }}-backend-config"}'
    {{- else }}
    cloud.google.com/backend-config: '{"ports": {"{{ .Values.browseHouse.port }}": "{{ .Values.browseHouse.name }}-backend-config"}}'
    {{- end }}
  {{- end }}
spec:
  type: {{ .Values.serviceType | default "ClusterIP" }}
  selector:
    integrify.service: {{ .Values.browseHouse.name }}
  ports:
    - protocol: TCP
      port: {{ .Values.browseHouse.port }}
      targetPort: {{ .Values.browseHouse.port }}
status:
  loadBalancer: {}
